# Challenge 2.2 Solution

1. Parse all log entries (JSON, CSV, YAML formats)
2. Extract node number and sigil from each
3. Sort by node number ascending
4. Convert each 16-bit hex sigil to two ASCII bytes (high byte, low byte)
5. Concatenate: REACTIVE ALL!
